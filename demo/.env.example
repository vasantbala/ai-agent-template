# Agent to connect to. When running via docker-compose.demo.yml this is set
# automatically to http://agent:8000. Override for a remote agent.
AGENT_URL=http://localhost:8000

# Optional API key — sent as X-API-Key header if set.
# Only needed when AUTH__ENABLED=true on the agent.
AGENT_API_KEY=
